<%= form_with(model: legendary_action, local: true) do |form| %>
  <% if legendary_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(legendary_action.errors.count, "error") %> prohibited this legendary_action from being saved:</h2>

      <ul>
      <% legendary_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :attack_bonus %>
    <%= form.number_field :attack_bonus %>
  </div>

  <div class="field">
    <%= form.label :damage_dice %>
    <%= form.text_field :damage_dice %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
