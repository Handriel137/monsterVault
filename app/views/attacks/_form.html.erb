<%= form_with(model: attack, local: true) do |form| %>
  <% if attack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attack.errors.count, "error") %> prohibited this attack from being saved:</h2>

      <ul>
      <% attack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :damage_dice %>
    <%= form.text_field :damage_dice %>
  </div>

  <div class="field">
    <%= form.label :damage_bonus %>
    <%= form.number_field :damage_bonus %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
